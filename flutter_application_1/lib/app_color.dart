import 'package:flutter/material.dart';

class AppColor {
  // Background (Almost white with blue tint)
  static const Color background = Color(0xFFFCFCFF);
  
  // Primary (Olive green)
  static const Color primary = Color(0xFFA1AA12);
  
  // Accent (Light grayish beige)
  static const Color accent = Color(0xFFC3BABA);
  
  // Text (Medium gray)
  static const Color text = Color(0xFF969292);
  
  // Optional: Add variants if needed
  static const Color primaryDark = Color(0xFF888F0F);
  static const Color primaryLight = Color(0xFFB4BE14);
}